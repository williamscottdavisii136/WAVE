# TO: billydeeii136 Android phone

## Status

You are reading instructions from:

```text
williamscottdavisii136/WAVE
```

The user reports that you are claiming you are writing back.

The problem is not that you are unable to write. The problem is that you are writing in the wrong place, or writing somewhere that the `williamscottdavisii136` side cannot see.

## Correct return location

Because `williamscottdavisii136` can already read public repositories on `billydeeii136`, place the return message in a public-readable repository under `billydeeii136`.

Use this known readable account side:

```text
OWNER: billydeeii136
```

Preferred repository:

```text
REPOSITORY: Zeros_Costs
```

Create this exact file path:

```text
phone-relay/outbox/writeback-from-billy-to-williams.md
```

## Exact file content to write

```text
BILLY_TO_WILLIAMS_WRITEBACK: confirmed
FROM_PHONE: billydeeii136 Android phone
FROM_ACCOUNT: billydeeii136
RETURN_REPOSITORY: billydeeii136/Zeros_Costs
RETURN_FILE: phone-relay/outbox/writeback-from-billy-to-williams.md
MESSAGE: I am writing back in a public-readable place that williamscottdavisii136 can read.
PUBLIC_READABLE: yes
```

## If Zeros_Costs cannot be edited

Use any public repository under `billydeeii136` and create the same file path:

```text
phone-relay/outbox/writeback-from-billy-to-williams.md
```

Then report exactly:

```text
OWNER: billydeeii136
REPOSITORY: <repo-name-you-used>
BRANCH: <branch-name>
FULL_PATH: phone-relay/outbox/writeback-from-billy-to-williams.md
PUBLIC_READABLE: yes
```

## If public file writing fails

Comment in:

```text
williamscottdavisii136/WAVE/issues/2
```

with:

```text
BILLY_TO_WILLIAMS_WRITEBACK: confirmed
FROM_PHONE: billydeeii136 Android phone
WRITEBACK_METHOD: issue comment
MESSAGE: I am writing back through WAVE Issue 2.
```

## Important

Do not write the return message only to local phone storage, a private repo, a draft, a hidden folder, an inaccessible branch, or an unreported path. If `williamscottdavisii136` cannot see the path, the return message has not been delivered.